{
  "name": "sistem-pos",
  "version": "1.0.0",
  "description": "Sistem Point of Sales Real-Time Multi-Tenant",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/electron && npm run dev",
    "dev:web": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "dev:electron": "cd packages/electron && npm run dev",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend && npm run build --workspace=electron",
    "install:all": "npm install && npm install --workspace=backend && npm install --workspace=frontend && npm install --workspace=electron",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "author": "Sistem POS Team",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "form-data": "^4.0.4",
    "minio": "^8.0.6",
    "mysql2": "^3.14.5",
    "node-fetch": "^3.3.2"
  }
}
