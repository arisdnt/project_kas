# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=kasir

# MinIO Object Storage Configuration
# Set to true to enable MinIO file storage
MINIO_ENABLED=true
# MinIO server endpoint (without http://)
MINIO_ENDPOINT=127.0.0.1
# MinIO server port
MINIO_PORT=9000
# Use SSL/TLS for MinIO connection
MINIO_USE_SSL=false
# MinIO access credentials
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=your_minio_secret_key
# Default bucket name for file storage
MINIO_BUCKET=pos-files
# MinIO region (optional)
MINIO_REGION=us-east-1
# Presigned URL expiry time in seconds (5 minutes default)
MINIO_PRESIGN_EXPIRY=300

# JWT Configuration
JWT_SECRET=your-very-long-secure-jwt-secret-key-here
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3002,http://localhost:3003
SOCKET_CORS_ORIGIN=http://localhost:3002,http://localhost:3003

# File Upload Configuration (Optional - handled by MinIO config above)
# Maximum file size in bytes (50MB default)
MAX_FILE_SIZE=52428800
# Allowed file types (comma-separated MIME types)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,image/gif,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document